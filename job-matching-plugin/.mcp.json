{
  "mcpServers": {
    "facebook_crawler": {
      "type": "stdio",
      "command": "python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/facebook_crawler_server.py"],
      "env": {
        "JOB_MATCHING_WORKSPACE_ROOT": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
